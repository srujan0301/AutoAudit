name: "Short Test Workflow"

on:
  push:
    paths:
      - ".github/workflows/short-test.yml"  # Only trigger when this file changes

jobs:
  quick-job:
    runs-on: ubuntu-latest
    steps:
      - name: Quick echo
        run: echo "This is a short workflow for testing cleanup"

  #
